export type User = {
    name: string;
    lastname: string;
    address: string;
    email: string;
    password: string;
}

export type Product = {
    name: string;
    description: string;
    price: number;
    stock: number;
    classification: string;
    imageUrl: string;
}